# python对xml的解析
# 1.SAX SAX用时间驱动模型，通过在解析XML的过程中出发一个个的时间并调用用户定义的回调函数来处理XML文件
# 2.DOM 将XML数据在内存中解析成一个树，通过对树的操作来操作XML
# 3.ElementTree(元素树)轻量级的DOM，DOM需要将XML数据映射到内存中的树，一是比较慢，而是比较耗内存，而SAX流式读取xml文件，比较快，占用内存少，
# 需要用户实现回调函数。

# SAX
利用SAX解析XML文档牵涉到两个部分：解析器和事件处理器。
解析器负责服务XML文档，并向时间处理器发送时间，如元素开始和元素结束时间
事件处理器则负责对时间做出相应，对传递的XML数据进行处理

# ElementTree
ET将整个XML文档转化为树，Element则代表着树上的单个节点。对整个XML文档的交互（读取，写入，查找需要的元素），一般是在ElementTree
层面进行。对单个xml元素及其子元素，则是在Elenent层面进行的。









